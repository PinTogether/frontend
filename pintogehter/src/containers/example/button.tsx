import { Component } from "react";
import styles from "@/styles/example/button.module.scss";

export default class Button extends Component {
  render() {
    return (
      <div>
        <button>button</button>
      </div>
    );
  }
}
